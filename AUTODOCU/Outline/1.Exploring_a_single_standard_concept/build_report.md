# Build report

Advisory notes from the last `autodocu build` (section `1.Exploring_a_single_standard_concept`). This file is generated (overwritten every build); your Outline input was **not** modified by the build itself.

**This section's Outline is now clean** — all previously flagged items have been addressed in `Outline/1.Exploring_a_single_standard_concept/README.md`. The most recent fix narrowed the *Person Counts* step: switching to Person Counts rescales only the **time plot** and relabels the **Hierarchy tree** nodes to PC/DPC, while the **List** counts and the **Sex / Age / Visit Type** filters stay record-based (the earlier wording claimed all four changed).

All 17 steps ran against the live app and all 17 screenshots were captured from a passing run.
