# Build report — root `Outline/README.md`

Advisory notes from the last `autodocu build` (run for section `2.Exploring_a_single_non-standard_concept`). This file is generated and is refreshed on every build regardless of which section was built; it comments **only** on the root `Outline/README.md`. Your Outline input was **not** modified.

**The root Outline is clean.** Nothing had to be inferred or defaulted:

- **`# Run`** — all fields present: command (`docker run --rm -p 8563:8563 ehr_browser`), URL (`http://localhost:8563/`), port `8563`, and window size `1280 × 800`. Nothing was defaulted.
- **`# Index`** — clear: heading "Overview and use cases", one list entry per section as `**<name>**: <description>`. Applied as written; section 2's entry was added.
- **`# Documentation instruction`** — clear and applied throughout: British English; **EHRBrowser**, **OMOP-CDM**, **Concept**, **Concept Set** rendered bold with an upper-case start; acronyms expanded on first use, e.g. **RC** (Record Counts) / **DRC** (Descendant Record Counts).

Per-section notes live next to each section's input, in `Outline/<section>/build_report.md`.
