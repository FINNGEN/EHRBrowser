# Name

Replace with the app name.

# Description

Replace with a short prose description of what the app does and what the
documentation should help a reader understand.

# Index

Replace with instructions for how to build the index on the Documentation
root page: what to call the index section, whether it is a list or a table,
and what each entry shows.

For example:

```
Name the index section as 'Use cases'.
Show the subsections as a list with the subsection name ':' and a brief description.

## Use cases
- Explore a single code: How to search for a concept and read the charts.
```

# Run

Replace with how to launch the app before tests run, and how to reach it:

- The run command (e.g. a `docker run ...` line, or `npm start`).
- The URL to navigate to (e.g. http://localhost:8563/).
- The port.
- The browser window size / viewport to use. If omitted, `1280x800` is used.

For example:

```
Launch the app with:

    docker run --rm -p 8563:8563 my_app

Navigate to http://localhost:8563/
Window size: 1280x800
```

# Documentation instruction

*(Optional — delete this section if not needed.)*

Replace with global guidance on tone, language, and rendering conventions
that apply to every documentation page — for example the highlight style to
use instead of the default red box, or the language to write the docs in.
