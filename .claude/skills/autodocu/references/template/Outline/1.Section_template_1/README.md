# Name

Replace with the name of this documentation chapter.

# Description

Replace with an overall description of what this chapter walks the reader
through.

# Sections

Replace the headings and steps below with the real ones. The `##`/`###`
heading hierarchy here is reproduced exactly on the rendered documentation
page. Under each heading, write a bullet list of steps: navigation actions,
areas to describe, and the two keywords `highlight` and `take-screenshot`.

## First area

- Navigate / act on the app (e.g. "Use the 'Search' field to search for 'X'").
- `highlight` the named area of the app to draw a red box around it.
- `take-screenshot` to capture the current state into this section's
  `screenshots/` folder.
- Describe the area so the doc-writing step has prose to render.

### A sub-area

- `highlight` the sub-area and `take-screenshot`.
- Describe briefly what this sub-area shows.
